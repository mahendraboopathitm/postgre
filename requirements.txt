databricks-sdk>=0.20.0
pyyaml>=6.0
pydantic>=2.0
python-dotenv>=1.0.0
# Only needed for `scripts/cli.py`'s --github-repo publishing path (GitHub App JWT + API calls).
PyJWT[crypto]>=2.8.0
requests>=2.31.0
